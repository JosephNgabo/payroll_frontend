import { EmployeeModificationComponent } from './employee-modification.component'

describe('EmployeeModificationComponent', () => {
  it('should mount', () => {
    cy.mount(EmployeeModificationComponent)
  })
})